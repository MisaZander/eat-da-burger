<div id="{{id}}" class="col-12 text-center">
    <div class="row">
        <div class="col-9 text-center">
            <p>{{burger_name}}</p>
        </div>
        <div class="col-3 text-center">
            <button class="btn btn-primary devour-it" data-id="{{id}}">
                Devour It!
            </button>
        </div>
    </div>
</div>